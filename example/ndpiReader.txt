(main) : Line 4270
	//Unit-Test
		sesUnitTest(); 
      * function
        ndpi_ses_init()
        ndpi_ses_add_value()

      * description
        # Session Unit Test の略かも
        # 単一の指数平滑化方というワードが出てきた
        # 現時点ではよくわからない少数点を含むランダムな数字を計算するためのfor文に入れていた。
        # 結果的には、OK | ANOMALY が帰るようになっていた。

    desUnitTest();
      * function
        ndpi_des_init()
        ndpi_des_add_value()

      * description
        # Destination Unit Test の略かも
        # sesUnitTest()と同様、ランダムな数字を計算するためのfor文に入れていた。

    jitterUnitTest();
      * function
        ndpi_jitter_init()
        ndpi_jitter_add_value()
    
      * description
        # Jitter Unit Test 
        # 連続したPing値の平均を出すユニットのテストかも

    rsiUnitTest();
      * function
        ndpi_free_rsi()   __#多分メモリ確保と開放
        ndpi_alloc_rsi()  _/

        ndpi_rsi_add_value()

      * description
        # RSI (Relative Strength Index) Unit Test の略かも
        # RSIとは株とかで出てくる計算方法らしく、株の買われすぎ、売られすぎを数値で把握できるようにするものらしい。
        # なんでここで出てくるかはよくわかってない。
      
    hashUnitTest();
      * function
        ndpi_hash_alloc()  __#多分メモリ確保と開放
        ndpi_hash_free()   _/
      
        ndpi_hash_add_entry()
        ndpi_hash_find_entry()

      * description
        # alloc とか free とか出てきたので、メモリを確保する系の関数だと思われる。
        # hash値の値を格納するのかな？

    dgaUnitTest();
      * function
        ndpi_init_detection_module
        ndpi_set_protocol_detection_bitmask2
        ndpi_finalize_initialization
        ndpi_check_dga_name
        ndpi_exit_detection_module

      * description
        # DGA (Domain Generation Algorithm) Unit Test の略かも
        # dga という 自動生成されたドメインリストの配列と、
        # non-dga という 人の手で作成されたドメインリストの配列を、
        # それぞれ ndpi_check_dga_name() という関数に入れてチェックするテストだと思われる。

        # DGA は マルウェア攻撃に使用するドメインを次々と切り替えるためなどに用いられる場合があるそう。

    hllUnitTest();
      * function
        ndpi_hll_init()
        ndpi_hll_add_number()
        ndpi_hll_destroy()

      * description
        # HyperLogLog Unit Test の略かも (HyperLogLogってなんぞや)
        # src/lib/third_party/src/hll/hll.c を見るとソースコードに 
        # HyperLogLogって書いてあった。

        # 調べたらちゃんとそういうのがあった。
        # HyperLogLogとは、データセットの中から効率よく重複を除いた要素の数を
        # 見積もるためのものらしい。
        # 高い効率を得るために正確さを少しだけ犠牲にしてるらしい。

          # どのぐらい正確で効率が良いかというと、10億個以上の要素からなるデータから
          # 2% 程度の誤差で要素数を見積もるのに1.5kB 程度のメモリしか消費しないらしい。
          # 脱線したけど面白かった。
          # 参考ページ : https://jp.quora.com/HyperLogLog-とは何ですか

    bitmapUnitTest();
      * define
        NDPI_ZERO_BIT()
        NDPI_SET_BIT()
        NDPI_ISSET_BIT()
        NDPI_ISSET_BIT()

      * description
        # bitmap ってあの画像とかのbitmap?
        # define NDPI_ISSET_BIT(num, n)  (num & (1UL << ( n )))
        # まだ unsigned long (UL) についての理解がない
        
    automataUnitTest();
      * function
        ndpi_init_automa()
        ndpi_add_string_to_automa()
        ndpi_finalize_automa()
        ndpi_match_string()
        ndpi_free_automa()

      * description
        # よくわかんないけど自動で文字列を追加するとか？？

    analyzeUnitTest();
      * function
        ndpi_alloc_data_analysis()
        ndpi_free_data_analysis()
        ndpi_data_add_value()
    
      * description
        # これもよくわからなかった。 自動で何かするUnitのテストかな
    
    ndpi_self_check_host_match();
      * description
        # error message を printfするところを見ると、重複してる文字列を見つけるための
        # unit test かも

    analysisUnitTest();
    rulesUnitTest();

